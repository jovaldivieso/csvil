dt: 0.1
max_vel: 0.5

db_lacam:
  mode: replan
  replan_freq: 10
  time_limit_ms: 60000
  algorithm_config: planning/dblacam_algorithm_default.yaml

  environment:
    min: [-6.0, -6.0]
    max: [6.0, 6.0]
